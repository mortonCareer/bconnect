openapi: 3.0.3
info:
  title: Bconnect API
  description: 인테리어 종사자를 위한 하도급 섭외 · 관리 플랫폼 '품앗이'의 REST API
  version: 0.2.0

servers:
  - url: http://localhost:8080
    description: 로컬 개발 서버
  - url: https://api.bconnect.to
    description: 프로덕션 서버

security:
  - bearerAuth: []

tags:
  - name: Auth
    description: 인증 및 토큰 관리
  - name: Members
    description: 회원 정보 관리
  - name: Feeds
    description: 피드 관리
  - name: Posts
    description: 게시글 관리
  - name: Tasks
    description: 작업 사항 관리
  - name: Profiles
    description: 기술자 프로필 관리
  - name: Credentials
    description: 인증뱃지(자격/면허/증명서) 관리
  - name: Coworkers
    description: 동료 관계 관리
  - name: CoworkerRequests
    description: 동료 요청 관리
  - name: Recommendations
    description: 추천서 관리
  - name: Chats
    description: 채팅 관리
  - name: Devices
    description: 푸시 알림용 디바이스(브라우저) 토큰 관리
  - name: Attachments
    description: 파일 업로드/다운로드 (presign·confirm)
  - name: Storages
    description: 저장소(폴더) 관리 — Storage + Attachment 매핑

externalDocs:
  description: 개발 문서 (Notion)
  url: https://www.notion.so/morton-so/2dc965d2888b80ac9b72ca4d22812b12

paths:
  # =====================
  # Auth Endpoints
  # =====================
  /api/v1/auth/otp/send:
    post:
      operationId: sendOtp
      tags: [Auth]
      security: []
      summary: 인증코드 발송
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendCodeRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendOtpResponse'
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다
        '429':
          description: |
            - `A001` 일일 발송 한도를 초과했습니다
            - `A002` 재전송 대기 시간이 지나지 않았습니다

  /api/v1/auth/otp/verify:
    post:
      operationId: verifyOtp
      tags: [Auth]
      security: []
      summary: 인증코드 검증
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyCodeRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/VerifyOtpLoginResponse'
                  - $ref: '#/components/schemas/VerifyOtpSignupResponse'
                discriminator:
                  propertyName: registered
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다
            - `A003` 유효하지 않은 인증번호입니다
            - `A004` 인증번호가 만료되었습니다
            - `A005` 인증 시도 횟수를 초과했습니다

  /api/v1/auth/refresh:
    post:
      operationId: refreshToken
      tags: [Auth]
      security:
        - cookieAuth: []
      summary: 토큰 갱신
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RefreshTokenResponse'
        '401':
          description: |
            - `A006` 유효하지 않은 리프레시 토큰입니다

  /api/v1/auth/logout:
    post:
      operationId: logout
      tags: [Auth]
      summary: 로그아웃
      responses:
        '200':
          description: OK

  # =====================
  # Member Endpoints
  # =====================
  /api/v1/members:
    get:
      operationId: getMembers
      tags: [Members]
      summary: 회원 목록 조회 (ADMIN)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Member'

    post:
      operationId: registerMember
      tags: [Members]
      security: []
      summary: 회원가입
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterMemberRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다
            - `A009` 유효하지 않은 가입 토큰입니다
            - `A010` 가입 토큰이 만료되었습니다

  /api/v1/members/roles:
    get:
      operationId: getRoles
      tags: [Members]
      security: []
      summary: 역할 목록 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Role'

  /api/v1/members/check-username:
    get:
      operationId: checkUsername
      tags: [Members]
      security: []
      summary: 사용자명 중복 확인
      parameters:
        - name: username
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckUsernameResponse'
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/members/me:
    get:
      operationId: getMyMember
      tags: [Members]
      summary: 내 정보 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Member'

    put:
      operationId: updateMyMember
      tags: [Members]
      summary: 내 정보 수정
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMemberRequest'
      responses:
        '200':
          description: OK
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

    delete:
      operationId: withdraw
      tags: [Members]
      summary: 회원 탈퇴
      responses:
        '200':
          description: OK

  # =====================
  # Feed Endpoints
  # =====================
  /api/v1/feeds:
    get:
      operationId: getFeeds
      tags: [Feeds]
      security: []
      summary: 피드 목록 조회
      parameters:
        - name: profileId
          in: query
          required: false
          schema:
            type: integer
            format: int64
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: reverse
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeedOffsetPage'

  /api/v1/feeds/{feedId}:
    get:
      operationId: getFeed
      tags: [Feeds]
      security: []
      summary: 피드 단건 조회
      parameters:
        - name: feedId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Feed'

  # =====================
  # Post Endpoints
  # =====================
  /api/v1/posts:
    post:
      operationId: createPost
      tags: [Posts]
      summary: 게시글 생성
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePostRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/posts/{postId}:
    parameters:
      - name: postId
        in: path
        required: true
        schema:
          type: integer
          format: int64
    put:
      operationId: updatePost
      tags: [Posts]
      summary: 게시글 수정
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePostRequest'
      responses:
        '200':
          description: OK
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

    delete:
      operationId: deletePost
      tags: [Posts]
      summary: 게시글 삭제
      responses:
        '200':
          description: OK

  # =====================
  # Task Endpoints
  # =====================
  /api/v1/tasks:
    get:
      operationId: getMyTasks
      tags: [Tasks]
      summary: 내 작업 목록 조회
      parameters:
        - name: start
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: end
          in: query
          required: true
          schema:
            type: string
            format: date
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Task'

    post:
      operationId: createTask
      tags: [Tasks]
      summary: 작업 생성
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/coworkers/tasks:
    get:
      operationId: getCoworkerTasks
      tags: [Tasks]
      summary: 지인 작업목록 조회
      parameters:
        - name: start
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: end
          in: query
          required: true
          schema:
            type: string
            format: date
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CoworkerTask'

  /api/v1/tasks/{taskId}:
    parameters:
      - name: taskId
        in: path
        required: true
        schema:
          type: integer
          format: int64
    put:
      operationId: updateTask
      tags: [Tasks]
      summary: 작업 수정
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTaskRequest'
      responses:
        '200':
          description: OK
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

    delete:
      operationId: deleteTask
      tags: [Tasks]
      summary: 작업 삭제
      responses:
        '200':
          description: OK

  # =====================
  # Profile Endpoints
  # =====================
  /api/v1/profiles/trades:
    get:
      operationId: getTrades
      tags: [Profiles]
      security: []
      summary: 공종 목록 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Trade'

  /api/v1/profiles:
    get:
      operationId: getProfiles
      tags: [Profiles]
      security: []
      summary: 프로필 목록 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProfileAndMember'

    post:
      operationId: createProfile
      tags: [Profiles]
      summary: 프로필 생성
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProfileRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/profiles/{profileId}:
    get:
      operationId: getProfile
      tags: [Profiles]
      security: []
      summary: 프로필 조회
      parameters:
        - name: profileId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProfileAndMember'

  /api/v1/profiles/me:
    put:
      operationId: updateMyProfile
      tags: [Profiles]
      summary: 내 프로필 수정
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProfileRequest'
      responses:
        '200':
          description: OK
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

    delete:
      operationId: deleteMyProfile
      tags: [Profiles]
      summary: 내 프로필 삭제
      responses:
        '200':
          description: OK

  /api/v1/profiles/me/about:
    put:
      operationId: updateMyProfileAbout
      tags: [Profiles]
      summary: 내 소개 수정
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProfileAboutRequest'
      responses:
        '200':
          description: OK

  # =====================
  # Credential Endpoints
  # =====================
  /api/v1/credentials/types:
    get:
      operationId: getCredentialTypes
      tags: [Credentials]
      security: []
      summary: 인증뱃지 타입 목록 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CredentialType'

  /api/v1/credentials:
    get:
      operationId: getCredentials
      tags: [Credentials]
      security: []
      summary: 인증뱃지 목록 조회
      parameters:
        - name: profileId
          in: query
          required: true
          description: 프로필 ID
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Credential'

    post:
      operationId: createCredential
      tags: [Credentials]
      summary: 인증뱃지 등록
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCredentialRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/credentials/{credentialId}/accept:
    post:
      operationId: acceptCredential
      tags: [Credentials]
      summary: 인증뱃지 승인 (ADMIN)
      parameters:
        - name: credentialId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/credentials/{credentialId}/deny:
    post:
      operationId: denyCredential
      tags: [Credentials]
      summary: 인증뱃지 거부 (ADMIN)
      parameters:
        - name: credentialId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/credentials/{credentialId}:
    delete:
      operationId: deleteCredential
      tags: [Credentials]
      summary: 인증뱃지 삭제
      parameters:
        - name: credentialId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  # =====================
  # Coworker Endpoints
  # =====================
  /api/v1/coworkers:
    get:
      operationId: getCoworkers
      tags: [Coworkers]
      summary: 동료 목록 조회
      parameters:
        - name: profileId
          in: query
          required: true
          description: 프로필 ID
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Coworker'

  /api/v1/coworkers/{coworkerId}:
    delete:
      operationId: deleteCoworker
      tags: [Coworkers]
      summary: 동료 삭제
      parameters:
        - name: coworkerId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  # =====================
  # Chat Endpoints
  # =====================
  /api/v1/chats:
    get:
      operationId: getMyChats
      tags: [Chats]
      summary: 내 채팅방 목록 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Chat'

  /api/v1/chats/direct:
    post:
      operationId: createDirectChat
      tags: [Chats]
      summary: 일대일 채팅 생성
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDirectChatRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Chat'

  /api/v1/chats/{chatId}/messages:
    get:
      operationId: getChatMessages
      tags: [Chats]
      summary: 채팅방 메시지 목록 조회
      parameters:
        - name: chatId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: cursor
          in: query
          required: false
          description: 페이지네이션 커서 (null이면 최신 메시지부터)
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: reverse
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageCursorPage'

  # =====================
  # Attachment Endpoints
  # =====================
  /api/v1/attachments/presign:
    post:
      operationId: presignAttachments
      tags: [Attachments]
      summary: 첨부 업로드용 Presigned PUT URL 발급
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PresignRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PresignResponse'
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/attachments/confirm:
    post:
      operationId: confirmAttachments
      tags: [Attachments]
      summary: 업로드 완료 확인
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfirmResponse'
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  # =====================
  # Storage Endpoints
  # =====================
  /api/v1/storages:
    get:
      operationId: getStorages
      tags: [Storages]
      summary: 저장소 목록 조회
      parameters:
        - name: type
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/StorageType'
        - name: projectId
          in: query
          required: false
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Storage'
    post:
      operationId: createStorage
      tags: [Storages]
      summary: 저장소 생성
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateStorageRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/storages/{storageId}:
    parameters:
      - name: storageId
        in: path
        required: true
        schema:
          type: integer
          format: int64
    get:
      operationId: getStorage
      tags: [Storages]
      summary: 저장소 상세 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Storage'
    delete:
      operationId: deleteStorage
      tags: [Storages]
      summary: 저장소 삭제
      responses:
        '200':
          description: OK

  /api/v1/storages/{storageId}/attachments:
    parameters:
      - name: storageId
        in: path
        required: true
        schema:
          type: integer
          format: int64
    post:
      operationId: addStorageAttachments
      tags: [Storages]
      summary: 저장소에 첨부 추가
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddStorageAttachmentsRequest'
      responses:
        '200':
          description: OK

  # =====================
  # Recommendation Endpoints
  # =====================
  /api/v1/recommendations:
    post:
      operationId: createRecommendation
      tags: [Recommendations]
      summary: 추천서 보내기
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRecommendationRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64

  /api/v1/recommendations/received:
    get:
      operationId: getReceivedRecommendations
      tags: [Recommendations]
      security: []
      summary: 받은 추천서 목록 조회
      parameters:
        - name: profileId
          in: query
          required: true
          description: 프로필 ID
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Recommendation'

  /api/v1/recommendations/sent:
    get:
      operationId: getSentRecommendations
      tags: [Recommendations]
      security: []
      summary: 보낸 추천서 목록 조회
      parameters:
        - name: profileId
          in: query
          required: true
          description: 프로필 ID
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Recommendation'

  /api/v1/recommendations/me/received:
    get:
      operationId: getMyReceivedRecommendations
      tags: [Recommendations]
      summary: 내가 받은 추천서 목록 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Recommendation'

  /api/v1/recommendations/me/sent:
    get:
      operationId: getMySentRecommendations
      tags: [Recommendations]
      summary: 내가 보낸 추천서 목록 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Recommendation'

  /api/v1/recommendations/{id}:
    put:
      operationId: updateRecommendation
      tags: [Recommendations]
      summary: 추천서 수정
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRecommendationRequest'
      responses:
        '200':
          description: OK

    delete:
      operationId: deleteRecommendation
      tags: [Recommendations]
      summary: 추천서 삭제
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/recommendations/{id}/hide:
    post:
      operationId: hideRecommendation
      tags: [Recommendations]
      summary: 추천서 숨김
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/recommendations/{id}/show:
    post:
      operationId: showRecommendation
      tags: [Recommendations]
      summary: 추천서 숨김 해제
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  # =====================
  # CoworkerRequest Endpoints
  # =====================
  /api/v1/coworker-requests:
    post:
      operationId: createCoworkerRequest
      tags: [CoworkerRequests]
      summary: 동료요청 생성
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCoworkerRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/coworker-requests/received:
    get:
      operationId: getReceivedCoworkerRequests
      tags: [CoworkerRequests]
      summary: 받은 동료 요청 목록 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CoworkerRequest'

  /api/v1/coworker-requests/sent:
    get:
      operationId: getSentCoworkerRequests
      tags: [CoworkerRequests]
      summary: 보낸 동료 요청 목록 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CoworkerRequest'

  /api/v1/coworker-requests/{id}:
    delete:
      operationId: cancelCoworkerRequest
      tags: [CoworkerRequests]
      summary: 동료요청 취소
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/coworker-requests/{id}/accept:
    post:
      operationId: acceptCoworkerRequest
      tags: [CoworkerRequests]
      summary: 동료요청 수락
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/coworker-requests/{id}/deny:
    post:
      operationId: denyCoworkerRequest
      tags: [CoworkerRequests]
      summary: 동료요청 거절
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/devices:
    post:
      operationId: registerDevice
      tags: [Devices]
      summary: 푸시 알림 디바이스 토큰 등록
      description: |
        FCM 디바이스 토큰을 현재 로그인한 회원에게 UPSERT.
        같은 토큰이면 `last_active_at` 만 갱신, 바뀌었으면 교체.
        앱 진입마다 호출되는 idempotent 엔드포인트.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterDeviceRequest'
      responses:
        '200':
          description: 등록/갱신 성공
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterDeviceResponse'
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다 (token/platform 누락)
    delete:
      operationId: unregisterDevice
      tags: [Devices]
      summary: 푸시 알림 디바이스 토큰 해제
      description: |
        로그아웃 또는 디바이스 알림 해제 시 호출.
        다른 사용자가 같은 브라우저에 로그인했을 때 이전 사용자의 알림이 가지 않도록 삭제.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnregisterDeviceRequest'
      responses:
        '204':
          description: 해제 성공 (응답 바디 없음)
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다 (token 누락)

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        인증이 필요한 엔드포인트에 공통으로 다음 에러를 반환할 수 있습니다.
        - `A007` 유효하지 않은 액세스 토큰입니다 (401)
        - `A008` 세션이 만료되었습니다 (401)
    cookieAuth:
      type: apiKey
      in: cookie
      name: refreshToken

  schemas:
    # =====================
    # Common - Error
    # =====================
    ApiError:
      type: object
      required: [code, status]
      properties:
        code:
          type: string
          example: 'C001'
        status:
          type: string
          example: 'BAD_REQUEST'

    ApiErrorResponse:
      type: object
      required: [success, error, data]
      properties:
        success:
          type: boolean
          enum: [false]
        error:
          $ref: '#/components/schemas/ApiError'
        data:
          nullable: true

    # =====================
    # Common
    Address:
      type: object
      required: [zipcode, city, state, street, latitude, longitude]
      properties:
        zipcode:
          type: string
          pattern: '\d{5}'
          example: '04524'
        city:
          type: string
        state:
          type: string
        street:
          type: string
        detail:
          type: string
          nullable: true
        latitude:
          type: number
          format: double
          example: 37.5666805
        longitude:
          type: number
          format: double
          example: 126.9784147

    Role:
      type: string
      enum:
        - GUEST
        - CLIENT
        - ARCHITECT
        - CONTRACTOR
        - FOREMAN
        - SKILLED
        - SEMI_SKILLED
        - HELPER
        - ADMIN

    Trade:
      type: string
      enum:
        # 기반 공정
        - DESIGN
        - DEMOLITION
        - ELECTRICAL
        - PLUMBING
        - MECHANICAL
        # 구조 공정
        - MASONRY
        - CARPENTRY
        - GLAZING
        - WATERPROOFING
        - PLASTERING
        - INSULATION
        # 마감 공정
        - TILING
        - GROUTING
        - PAINTING
        - WALLPAPER
        - FILM_SHEET
        - HARDWOOD
        - VINYL
        # 설치
        - SINK
        - FURNITURE
        - AIR_CONDITIONING
        # 현장지원
        - HOISTING
        - TRANSPORT
        - CLEANING
        - GENERAL_LABOR

    # =====================
    # Auth Schemas
    # =====================
    SendCodeRequest:
      type: object
      required: [phone]
      properties:
        phone:
          type: string
          pattern: '\d{10,11}'

    SendOtpResponse:
      type: object
      required: [expiresAt]
      properties:
        expiresAt:
          type: string
          format: date-time

    VerifyCodeRequest:
      type: object
      required: [phone, code]
      properties:
        phone:
          type: string
          pattern: '\d{10,11}'
        code:
          type: string
          pattern: '\d{6}'

    VerifyOtpLoginResponse:
      type: object
      required: [registered, accessToken, refreshToken]
      properties:
        registered:
          type: boolean
          enum: [true]
        accessToken:
          type: string
        refreshToken:
          type: string

    VerifyOtpSignupResponse:
      type: object
      required: [registered, signupToken]
      properties:
        registered:
          type: boolean
          enum: [false]
        signupToken:
          type: string

    RefreshTokenResponse:
      type: object
      required: [accessToken, refreshToken]
      properties:
        accessToken:
          type: string
        refreshToken:
          type: string

    # =====================
    # Member Schemas
    # =====================
    Member:
      type: object
      required: [id, username, name, phone, role, createdAt, modifiedAt]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        username:
          type: string
        name:
          type: string
        phone:
          type: string
        picture:
          type: string
          nullable: true
        role:
          $ref: '#/components/schemas/Role'
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    MaskedMember:
      type: object
      required: [id, username, name, createdAt, modifiedAt]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        username:
          type: string
        name:
          type: string
        picture:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    RegisterMemberRequest:
      type: object
      required: [signupToken, username, name, role]
      properties:
        signupToken:
          type: string
        username:
          type: string
        name:
          type: string
        picture:
          type: string
          format: uri
          nullable: true
        role:
          $ref: '#/components/schemas/Role'

    CheckUsernameResponse:
      type: object
      required: [available]
      properties:
        available:
          type: boolean

    UpdateMemberRequest:
      type: object
      required: [name, role]
      properties:
        name:
          type: string
        picture:
          type: string
          format: uri
          nullable: true
        role:
          $ref: '#/components/schemas/Role'

    # =====================
    # Profile Schemas
    # =====================
    Profile:
      type: object
      required: [id, memberId, primaryTrade, trades, experience, address, createdAt, modifiedAt]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        memberId:
          type: integer
          format: int64
          example: 1
        primaryTrade:
          $ref: '#/components/schemas/Trade'
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        experience:
          type: integer
        headline:
          type: string
          nullable: true
        about:
          type: string
          nullable: true
        address:
          $ref: '#/components/schemas/Address'
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    ProfileAndMember:
      type: object
      required: [member, profile]
      properties:
        member:
          $ref: '#/components/schemas/MaskedMember'
        profile:
          $ref: '#/components/schemas/Profile'

    UpdateProfileRequest:
      type: object
      required: [primaryTrade, trades, experience, address]
      properties:
        primaryTrade:
          $ref: '#/components/schemas/Trade'
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
          minItems: 1
        experience:
          type: integer
          minimum: 0
        headline:
          type: string
          nullable: true
        address:
          $ref: '#/components/schemas/Address'

    CreateProfileRequest:
      type: object
      required: [primaryTrade, trades, experience, address]
      properties:
        primaryTrade:
          $ref: '#/components/schemas/Trade'
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
          minItems: 1
        experience:
          type: integer
          minimum: 0
        headline:
          type: string
          nullable: true
        about:
          type: string
          nullable: true
        address:
          $ref: '#/components/schemas/Address'

    UpdateProfileAboutRequest:
      type: object
      properties:
        about:
          type: string
          nullable: true

    # =====================
    # Feed Schemas
    # =====================
    Feed:
      type: object
      required: [member, profile, post]
      properties:
        member:
          $ref: '#/components/schemas/MaskedMember'
        profile:
          $ref: '#/components/schemas/Profile'
        post:
          $ref: '#/components/schemas/Post'

    # =====================
    # Post Schemas
    # =====================
    Post:
      type: object
      required: [id, images, createdAt, modifiedAt]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        profileId:
          type: integer
          format: int64
          example: 1
          nullable: true
        taskId:
          type: integer
          format: int64
          example: 1
          nullable: true
        images:
          type: array
          items:
            type: string
        content:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    CreatePostRequest:
      type: object
      required: [images, content]
      properties:
        taskId:
          type: integer
          format: int64
          example: 1
          nullable: true
        images:
          type: array
          items:
            type: string
            format: uri
          minItems: 1
        content:
          type: string

    UpdatePostRequest:
      type: object
      required: [content]
      properties:
        content:
          type: string

    # =====================
    # Task Schemas
    # =====================
    Task:
      type: object
      required:
        [id, company, address, taskTitle, eventTitle, trades, start, end, createdAt, modifiedAt]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        profileId:
          type: integer
          format: int64
          example: 1
          nullable: true
        company:
          type: string
        address:
          $ref: '#/components/schemas/Address'
        taskTitle:
          type: string
        eventTitle:
          type: string
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        start:
          type: string
          format: date
        end:
          type: string
          format: date
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    CreateTaskRequest:
      type: object
      required: [company, address, taskTitle, eventTitle, trades, start, end]
      properties:
        company:
          type: string
        address:
          $ref: '#/components/schemas/Address'
        taskTitle:
          type: string
        eventTitle:
          type: string
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        start:
          type: string
          format: date
        end:
          type: string
          format: date

    CoworkerTask:
      type: object
      required: [id, profileId, start, end]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        profileId:
          type: integer
          format: int64
          example: 1
        start:
          type: string
          format: date
        end:
          type: string
          format: date

    UpdateTaskRequest:
      type: object
      required: [company, address, taskTitle, eventTitle, trades, start, end]
      properties:
        company:
          type: string
        address:
          $ref: '#/components/schemas/Address'
        taskTitle:
          type: string
        eventTitle:
          type: string
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        start:
          type: string
          format: date
        end:
          type: string
          format: date

    # =====================
    # Credential Schemas
    # =====================
    CredentialType:
      type: string
      enum:
        # 사업자 및 면허
        - IDENTITY_VERIFICATION
        - SOLE_PROPRIETOR
        - CONSTRUCTION_LICENSE
        - SPECIALTY_CONSTRUCTION_LICENSE
        # 증명서
        - CAREER_CERTIFICATE
        - SKILL_GRADE_CERTIFICATE
        - OTHER_CERTIFICATE
        # 자격증
        - NATIONAL_TECHNICAL_QUALIFICATION
        - SKILLED_TECHNICIAN
        - OTHER_QUALIFICATION

    CredentialStatus:
      type: string
      enum:
        - PENDING
        - ACCEPTED
        - DENIED

    Credential:
      type: object
      required: [id, profileId, type, status, createdAt, modifiedAt]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        profileId:
          type: integer
          format: int64
          example: 1
        type:
          $ref: '#/components/schemas/CredentialType'
        status:
          $ref: '#/components/schemas/CredentialStatus'
        expiredAt:
          type: string
          format: date
          nullable: true
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    CreateCredentialRequest:
      type: object
      required: [type]
      properties:
        type:
          $ref: '#/components/schemas/CredentialType'
        expiredAt:
          type: string
          format: date
          nullable: true

    # =====================
    # Coworker Schemas
    # =====================
    Coworker:
      type: object
      required: [id, minId, maxId]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        minId:
          type: integer
          format: int64
          example: 1
        maxId:
          type: integer
          format: int64
          example: 2

    # =====================
    # CoworkerRequest Schemas
    # =====================
    CoworkerRequest:
      type: object
      required: [id, fromId, toId]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        fromId:
          type: integer
          format: int64
          example: 1
        toId:
          type: integer
          format: int64
          example: 2

    CreateCoworkerRequest:
      type: object
      required: [toId]
      properties:
        toId:
          type: integer
          format: int64
          example: 1

    # =====================
    # Pagination Schemas
    # =====================
    FeedOffsetPage:
      type: object
      required: [content, hasNext]
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/Feed'
        hasNext:
          type: boolean

    MessageCursorPage:
      type: object
      required: [content, hasNext]
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/Message'
        nextCursor:
          type: string
          nullable: true
        hasNext:
          type: boolean

    # =====================
    # Chat Schemas
    # =====================
    MessageType:
      type: string
      enum:
        - TEXT
        - IMAGE
        - FILE
        - SYSTEM

    Chat:
      type: object
      required: [id, title, participantIds, lastMessage, unreadCount, createdAt, modifiedAt]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        title:
          type: string
        participantIds:
          type: array
          items:
            type: integer
            format: int64
        lastMessage:
          $ref: '#/components/schemas/Message'
        unreadCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    Message:
      type: object
      required: [id, chatId, memberId, type, content, createdAt, modifiedAt]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        chatId:
          type: integer
          format: int64
          example: 1
        memberId:
          type: integer
          format: int64
          example: 1
        type:
          $ref: '#/components/schemas/MessageType'
        content:
          type: string
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    CreateDirectChatRequest:
      type: object
      required: [participantId]
      properties:
        participantId:
          type: integer
          format: int64

    # =====================
    # Recommendation Schemas
    # =====================
    Recommendation:
      type: object
      required: [id, fromId, toId, content, visible, createdAt, modifiedAt]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        fromId:
          type: integer
          format: int64
          example: 1
        toId:
          type: integer
          format: int64
          example: 2
        content:
          type: string
        visible:
          type: boolean
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    CreateRecommendationRequest:
      type: object
      required: [toId, content]
      properties:
        toId:
          type: integer
          format: int64
        content:
          type: string

    UpdateRecommendationRequest:
      type: object
      required: [content]
      properties:
        content:
          type: string

    # =====================
    # Devices (Web Push)
    # =====================
    DevicePlatform:
      type: string
      enum: [web, android, ios]
      description: |
        디바이스 플랫폼.
        - `web`: 브라우저 Web Push (FCM JS SDK 발급 토큰)
        - `android`, `ios`: 추후 네이티브 앱 출시 시 사용

    RegisterDeviceRequest:
      type: object
      required: [token, platform]
      properties:
        token:
          type: string
          description: FCM 디바이스 토큰 (FCM SDK 가 발급한 고유 식별자, 150자 내외)
          example: 'eNkX...'
        platform:
          $ref: '#/components/schemas/DevicePlatform'

    RegisterDeviceResponse:
      type: object
      required: [registered]
      properties:
        registered:
          type: boolean
          description: true 고정 (등록/갱신 공통)

    UnregisterDeviceRequest:
      type: object
      required: [token]
      properties:
        token:
          type: string
          description: 해제할 FCM 디바이스 토큰

    # =====================
    # Attachment Schemas
    # =====================
    AttachmentStatus:
      type: string
      enum: [PENDING, COMPLETED, FAILED]

    AttachmentMeta:
      type: object
      required: [id, path, filename, contentType, size]
      properties:
        id:
          type: integer
          format: int64
        path:
          type: string
          description: S3 key 디렉토리 prefix (예 chats/42/images, credentials/7/files)
        filename:
          type: string
        contentType:
          type: string
        size:
          type: integer
          format: int64
          description: bytes

    PresignFileRequest:
      type: object
      required: [filename, size, contentType]
      properties:
        filename:
          type: string
        size:
          type: integer
          format: int64
        contentType:
          type: string

    PresignFileResponse:
      type: object
      required: [attachmentId, uploadUrl]
      properties:
        attachmentId:
          type: integer
          format: int64
        uploadUrl:
          type: string
          format: uri
          description: 클라이언트가 PUT으로 업로드할 S3 presigned URL

    PresignRequest:
      type: object
      required: [files]
      properties:
        files:
          type: array
          minItems: 1
          maxItems: 50
          items:
            $ref: '#/components/schemas/PresignFileRequest'

    PresignResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PresignFileResponse'

    ConfirmRequest:
      type: object
      required: [attachmentIds]
      properties:
        attachmentIds:
          type: array
          minItems: 1
          items:
            type: integer
            format: int64

    ConfirmItem:
      type: object
      required: [attachmentId, status]
      properties:
        attachmentId:
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/AttachmentStatus'
        attachment:
          $ref: '#/components/schemas/AttachmentMeta'
          nullable: true

    ConfirmResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ConfirmItem'

    # =====================
    # Storage Schemas
    # =====================
    StorageType:
      type: string
      enum: [PROJECT, MEMBER]

    Storage:
      type: object
      required: [id, type, title, createdAt, modifiedAt]
      properties:
        id:
          type: integer
          format: int64
        type:
          $ref: '#/components/schemas/StorageType'
        projectId:
          type: integer
          format: int64
          nullable: true
        memberId:
          type: integer
          format: int64
          nullable: true
        title:
          type: string
        attachments:
          type: array
          items:
            $ref: '#/components/schemas/AttachmentMeta'
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    CreateStorageRequest:
      type: object
      required: [type, title]
      properties:
        type:
          $ref: '#/components/schemas/StorageType'
        projectId:
          type: integer
          format: int64
          nullable: true
        title:
          type: string

    AddStorageAttachmentsRequest:
      type: object
      required: [attachmentIds]
      properties:
        attachmentIds:
          type: array
          minItems: 1
          items:
            type: integer
            format: int64
