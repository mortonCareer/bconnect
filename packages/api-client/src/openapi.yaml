openapi: 3.0.3
info:
  title: Morton API
  version: 1.0.0

servers:
  - url: http://localhost:8080

security:
  - bearerAuth: []

paths:
  # =====================
  # Auth Endpoints
  # =====================
  /api/v1/auth/otp/send:
    post:
      operationId: sendOtp
      tags: [Auth]
      security: []
      summary: SMS 인증코드를 발송합니다
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendCodeRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendOtpResponse'
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다
        '429':
          description: |
            - `A001` 일일 발송 한도를 초과했습니다
            - `A002` 재전송 대기 시간이 지나지 않았습니다

  /api/v1/auth/otp/verify:
    post:
      operationId: verifyOtp
      tags: [Auth]
      security: []
      summary: 인증코드를 검증합니다 (로그인 또는 회원가입 분기)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyCodeRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/VerifyOtpLoginResponse'
                  - $ref: '#/components/schemas/VerifyOtpSignupResponse'
                discriminator:
                  propertyName: registered
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다
            - `A003` 유효하지 않은 인증번호입니다
            - `A004` 인증번호가 만료되었습니다
            - `A005` 인증 시도 횟수를 초과했습니다

  /api/v1/auth/refresh:
    post:
      operationId: refreshToken
      tags: [Auth]
      security: []
      summary: 리프레시 토큰으로 액세스 토큰을 갱신합니다
      description: httpOnly 쿠키(refreshToken)에서 리프레시 토큰을 읽습니다. credentials include 필요.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RefreshTokenResponse'
        '401':
          description: |
            - `A006` 유효하지 않은 리프레시 토큰입니다

  /api/v1/auth/logout:
    post:
      operationId: logout
      tags: [Auth]
      summary: 로그아웃하고 리프레시 토큰을 무효화합니다
      responses:
        '200':
          description: OK

  # =====================
  # Member Endpoints
  # =====================
  /api/v1/members:
    get:
      operationId: getMembers
      tags: [Members]
      summary: 회원 목록을 조회합니다
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Member'

    post:
      operationId: registerMember
      tags: [Members]
      security: []
      summary: 새 회원을 등록합니다
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterMemberRequest'
      description: 회원 등록 후 accessToken 반환 + refreshToken httpOnly 쿠키 설정
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterMemberResponse'
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다
            - `A009` 유효하지 않은 가입 토큰입니다
            - `A010` 가입 토큰이 만료되었습니다

  /api/v1/members/me:
    get:
      operationId: getMyMember
      tags: [Members]
      summary: 내 회원 정보를 조회합니다
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Member'

    put:
      operationId: updateMyMember
      tags: [Members]
      summary: 내 회원 정보를 수정합니다
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMemberRequest'
      responses:
        '200':
          description: OK
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

    delete:
      operationId: withdrawMember
      tags: [Members]
      summary: 회원을 탈퇴합니다
      responses:
        '200':
          description: OK

  # =====================
  # Post Endpoints
  # =====================
  /api/v1/posts:
    get:
      operationId: getPosts
      tags: [Posts]
      security: []
      summary: 게시글 목록을 조회합니다 (필터, 커서 페이지네이션)
      parameters:
        - name: authorId
          in: query
          required: false
          description: 작성자 ID로 필터링
          schema:
            type: integer
            format: int64
        - name: taskId
          in: query
          required: false
          description: 작업 ID로 필터링
          schema:
            type: integer
            format: int64
        - name: trade
          in: query
          required: false
          description: 시공분야로 필터링
          schema:
            $ref: '#/components/schemas/Trade'
        - name: minExperience
          in: query
          required: false
          description: 최소 경력 (년)
          schema:
            type: integer
        - name: maxExperience
          in: query
          required: false
          description: 최대 경력 (년)
          schema:
            type: integer
        - name: cursor
          in: query
          required: false
          description: 페이지네이션 커서
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: 페이지 크기 (기본값 20)
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PostPage'

    post:
      operationId: createPost
      tags: [Posts]
      summary: 새 게시글을 작성합니다
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePostRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/posts/{postId}:
    get:
      operationId: getPost
      tags: [Posts]
      security: []
      summary: 게시글을 조회합니다
      parameters:
        - name: postId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Post'

    put:
      operationId: updatePost
      tags: [Posts]
      summary: 게시글을 수정합니다
      parameters:
        - name: postId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePostRequest'
      responses:
        '200':
          description: OK
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

    delete:
      operationId: deletePost
      tags: [Posts]
      summary: 게시글을 삭제합니다
      parameters:
        - name: postId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  # =====================
  # Task Endpoints
  # =====================
  /api/v1/tasks:
    get:
      operationId: getTasks
      tags: [Tasks]
      security: []
      summary: 작업 목록을 조회합니다
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Task'

    post:
      operationId: createTask
      tags: [Tasks]
      summary: 새 작업을 생성합니다
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/tasks/{taskId}:
    get:
      operationId: getTask
      tags: [Tasks]
      security: []
      summary: 작업을 조회합니다
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Task'

    put:
      operationId: updateTask
      tags: [Tasks]
      summary: 작업을 수정합니다
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTaskRequest'
      responses:
        '200':
          description: OK
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

    delete:
      operationId: deleteTask
      tags: [Tasks]
      summary: 작업을 삭제합니다
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  # =====================
  # Feed Endpoints
  # =====================
  /api/v1/feeds:
    get:
      operationId: getFeeds
      tags: [Feeds]
      security: []
      summary: 피드 목록 조회
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Feed'

  # =====================
  # Chat Endpoints
  # =====================
  /api/v1/chats:
    get:
      operationId: getMyChats
      tags: [Chats]
      summary: 내 채팅방 목록을 조회합니다 (커서 페이지네이션)
      parameters:
        - name: cursor
          in: query
          required: false
          description: 페이지네이션 커서
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: 페이지 크기 (기본값 20)
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatPage'

  /api/v1/chats/{chatId}:
    get:
      operationId: getChat
      tags: [Chats]
      summary: 채팅방을 조회합니다
      parameters:
        - name: chatId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Chat'

  /api/v1/chats/{chatId}/messages:
    get:
      operationId: getChatMessages
      tags: [Chats]
      summary: 채팅방의 메시지 목록을 조회합니다 (역방향 커서 페이지네이션)
      parameters:
        - name: chatId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: before
          in: query
          required: false
          description: 이 커서 이전의 메시지를 조회
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: 페이지 크기 (기본값 30)
          schema:
            type: integer
            default: 30
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessagePage'

  # =====================
  # Profile Endpoints
  # =====================
  /api/v1/profiles:
    get:
      operationId: getProfiles
      tags: [Profiles]
      security: []
      summary: 프로필 목록을 조회합니다
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Profile'

    post:
      operationId: createProfile
      tags: [Profiles]
      summary: 새 프로필을 생성합니다
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProfileRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/profiles/me:
    get:
      operationId: getMyProfile
      tags: [Profiles]
      summary: 내 프로필을 조회합니다
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Profile'

    put:
      operationId: updateMyProfile
      tags: [Profiles]
      summary: 내 프로필을 수정합니다
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProfileRequest'
      responses:
        '200':
          description: OK
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

    patch:
      operationId: updateMyProfileAbout
      tags: [Profiles]
      summary: 내 프로필 자기소개를 수정합니다
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProfileAboutRequest'
      responses:
        '200':
          description: OK

    delete:
      operationId: deleteMyProfile
      tags: [Profiles]
      summary: 내 프로필을 삭제합니다
      responses:
        '200':
          description: OK

  /api/v1/profiles/{profileId}:
    get:
      operationId: getProfile
      tags: [Profiles]
      security: []
      summary: 프로필을 조회합니다
      parameters:
        - name: profileId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Profile'

  # =====================
  # Credential Endpoints
  # =====================
  /api/v1/credentials:
    get:
      operationId: getCredentials
      tags: [Credentials]
      security: []
      summary: 자격증 목록을 조회합니다
      parameters:
        - name: profileId
          in: query
          required: true
          description: 프로필 ID
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Credential'

    post:
      operationId: createCredential
      tags: [Credentials]
      summary: 자격증을 등록합니다
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCredentialRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/credentials/{credentialId}:
    delete:
      operationId: deleteCredential
      tags: [Credentials]
      summary: 자격증을 삭제합니다
      parameters:
        - name: credentialId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/credentials/{credentialId}/accept:
    post:
      operationId: acceptCredential
      tags: [Credentials]
      summary: 자격증을 승인합니다 (ADMIN)
      parameters:
        - name: credentialId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/credentials/{credentialId}/deny:
    post:
      operationId: denyCredential
      tags: [Credentials]
      summary: 자격증을 거부합니다 (ADMIN)
      parameters:
        - name: credentialId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  # =====================
  # Coworker Endpoints
  # =====================
  /api/v1/coworkers:
    get:
      operationId: getCoworkers
      tags: [Coworkers]
      summary: 동료 목록을 조회합니다
      parameters:
        - name: profileId
          in: query
          required: true
          description: 프로필 ID
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Coworker'

  /api/v1/coworkers/{coworkerId}:
    delete:
      operationId: deleteCoworker
      tags: [Coworkers]
      summary: 동료를 삭제합니다
      parameters:
        - name: coworkerId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  # =====================
  # CoworkerRequest Endpoints
  # =====================
  /api/v1/coworker-requests:
    post:
      operationId: createCoworkerRequest
      tags: [CoworkerRequests]
      summary: 동료 요청을 보냅니다
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCoworkerRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: integer
                format: int64
        '400':
          description: |
            - `C001` 유효하지 않은 입력값입니다

  /api/v1/coworker-requests/{id}:
    delete:
      operationId: cancelCoworkerRequest
      tags: [CoworkerRequests]
      summary: 동료 요청을 취소합니다
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/coworker-requests/{id}/accept:
    post:
      operationId: acceptCoworkerRequest
      tags: [CoworkerRequests]
      summary: 동료 요청을 수락합니다
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

  /api/v1/coworker-requests/{id}/deny:
    post:
      operationId: denyCoworkerRequest
      tags: [CoworkerRequests]
      summary: 동료 요청을 거절합니다
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    # =====================
    # Common
    # =====================
    CursorPageMeta:
      type: object
      required: [hasMore]
      properties:
        nextCursor:
          type: string
          nullable: true
          description: 다음 페이지 커서 (마지막 페이지면 null)
        hasMore:
          type: boolean
          description: 다음 페이지 존재 여부

    Address:
      type: object
      properties:
        zipcode:
          type: string
          pattern: '\d{5}'
        city:
          type: string
        state:
          type: string
        street:
          type: string
        detail:
          type: string
        latitude:
          type: number
          format: double
        longitude:
          type: number
          format: double

    Role:
      type: string
      enum:
        - GUEST
        - CLIENT
        - ARCHITECT
        - CONTRACTOR
        - FOREMAN
        - SKILLED
        - SEMI_SKILLED
        - HELPER
        - ADMIN

    Trade:
      type: string
      enum:
        # 기반 공정
        - DESIGN
        - DEMOLITION
        - ELECTRICAL
        - PLUMBING
        - MECHANICAL
        # 구조 공정
        - MASONRY
        - CARPENTRY
        - GLAZING
        - WATERPROOFING
        - PLASTERING
        - INSULATION
        # 마감 공정
        - TILING
        - GROUTING
        - PAINTING
        - WALLPAPER
        - FILM_SHEET
        - HARDWOOD
        - VINYL
        # 설치
        - SINK
        - FURNITURE
        - AIR_CONDITIONING
        # 현장지원
        - HOISTING
        - TRANSPORT
        - CLEANING
        - GENERAL_LABOR

    # =====================
    # Auth Schemas
    # =====================
    SendCodeRequest:
      type: object
      required: [phone]
      properties:
        phone:
          type: string
          pattern: '\d{10,11}'

    SendOtpResponse:
      type: object
      properties:
        expiresAt:
          type: string
          format: date-time

    VerifyCodeRequest:
      type: object
      required: [phone, code]
      properties:
        phone:
          type: string
          pattern: '\d{10,11}'
        code:
          type: string
          pattern: '\d{6}'

    VerifyOtpLoginResponse:
      description: 로그인 성공 응답. refreshToken은 httpOnly 쿠키(refreshToken)로 전달됩니다.
      type: object
      required: [registered, accessToken]
      properties:
        registered:
          type: boolean
          enum: [true]
        accessToken:
          type: string

    VerifyOtpSignupResponse:
      type: object
      required: [registered, signupToken]
      properties:
        registered:
          type: boolean
          enum: [false]
        signupToken:
          type: string

    RefreshTokenResponse:
      type: object
      properties:
        accessToken:
          type: string

    RegisterMemberResponse:
      description: 회원 등록 응답. refreshToken은 httpOnly 쿠키로 전달됩니다.
      type: object
      required: [memberId, accessToken]
      properties:
        memberId:
          type: integer
          format: int64
        accessToken:
          type: string

    # =====================
    # Member Schemas
    # =====================
    Member:
      type: object
      properties:
        id:
          type: integer
          format: int64
        username:
          type: string
        name:
          type: string
        phone:
          type: string
        picture:
          type: string
        role:
          $ref: '#/components/schemas/Role'
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    RegisterMemberRequest:
      type: object
      required: [signupToken, username, name, phone, picture, role]
      properties:
        signupToken:
          type: string
        username:
          type: string
        name:
          type: string
        phone:
          type: string
          pattern: '\d{10,11}'
        picture:
          type: string
        role:
          $ref: '#/components/schemas/Role'

    UpdateMemberRequest:
      type: object
      required: [name, role]
      properties:
        name:
          type: string
        picture:
          type: string
        role:
          $ref: '#/components/schemas/Role'
    # =====================
    # Profile Schemas
    # =====================
    Profile:
      type: object
      properties:
        id:
          type: integer
          format: int64
        memberId:
          type: integer
          format: int64
        primaryTrade:
          $ref: '#/components/schemas/Trade'
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        experience:
          type: integer
        headline:
          type: string
          nullable: true
        about:
          type: string
          nullable: true
        address:
          $ref: '#/components/schemas/Address'
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    UpdateProfileRequest:
      type: object
      required: [primaryTrade, trades, address]
      properties:
        primaryTrade:
          $ref: '#/components/schemas/Trade'
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        experience:
          type: integer
        headline:
          type: string
        address:
          $ref: '#/components/schemas/Address'

    CreateProfileRequest:
      type: object
      required: [primaryTrade, trades, experience, address]
      properties:
        primaryTrade:
          $ref: '#/components/schemas/Trade'
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        experience:
          type: integer
        headline:
          type: string
        about:
          type: string
        address:
          $ref: '#/components/schemas/Address'

    UpdateProfileAboutRequest:
      type: object
      properties:
        about:
          type: string
    # =====================
    # Post Schemas
    # =====================
    Post:
      type: object
      properties:
        id:
          type: integer
          format: int64
        authorId:
          type: integer
          format: int64
        taskId:
          type: integer
          format: int64
        images:
          type: array
          items:
            type: string
        content:
          type: string
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    PostPage:
      type: object
      required: [items, meta]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Post'
        meta:
          $ref: '#/components/schemas/CursorPageMeta'

    CreatePostRequest:
      type: object
      required: [images, content]
      properties:
        taskId:
          type: integer
          format: int64
        images:
          type: array
          items:
            type: string
          minItems: 1
        content:
          type: string
    UpdatePostRequest:
      type: object
      required: [content]
      properties:
        content:
          type: string

    # =====================
    # Feed Schemas
    # =====================
    Feed:
      type: object
      required: [member, profile, post]
      properties:
        member:
          $ref: '#/components/schemas/FeedMember'
        profile:
          $ref: '#/components/schemas/FeedProfile'
        post:
          $ref: '#/components/schemas/FeedPost'

    FeedMember:
      type: object
      properties:
        id:
          type: integer
          format: int64
        username:
          type: string
        name:
          type: string
        picture:
          type: string
        role:
          $ref: '#/components/schemas/Role'
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    FeedProfile:
      type: object
      properties:
        id:
          type: integer
          format: int64
        primaryTrade:
          $ref: '#/components/schemas/Trade'
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        experience:
          type: integer
        headline:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    FeedPost:
      type: object
      properties:
        id:
          type: integer
          format: int64
        images:
          type: array
          items:
            type: string
        content:
          type: string
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    # =====================
    # Task Schemas
    # =====================
    Task:
      type: object
      properties:
        id:
          type: integer
          format: int64
        profileId:
          type: integer
          format: int64
        company:
          type: string
        address:
          $ref: '#/components/schemas/Address'
        taskTitle:
          type: string
        eventTitle:
          type: string
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        start:
          type: string
          format: date
        end:
          type: string
          format: date
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    CreateTaskRequest:
      type: object
      required: [company, address, taskTitle, eventTitle, trades, start, end]
      properties:
        company:
          type: string
        address:
          $ref: '#/components/schemas/Address'
        taskTitle:
          type: string
        eventTitle:
          type: string
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        start:
          type: string
          format: date
        end:
          type: string
          format: date

    UpdateTaskRequest:
      type: object
      required: [company, address, taskTitle, eventTitle, trades, start, end]
      properties:
        company:
          type: string
        address:
          $ref: '#/components/schemas/Address'
        taskTitle:
          type: string
        eventTitle:
          type: string
        trades:
          type: array
          items:
            $ref: '#/components/schemas/Trade'
          uniqueItems: true
        start:
          type: string
          format: date
        end:
          type: string
          format: date

    # =====================
    # Chat Schemas
    # =====================
    Chat:
      type: object
      properties:
        id:
          type: integer
          format: int64
        title:
          type: string
        participantIds:
          type: array
          items:
            type: integer
            format: int64
        lastMessage:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Message'
        unreadCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    Message:
      type: object
      properties:
        id:
          type: integer
          format: int64
        chatId:
          type: integer
          format: int64
        senderId:
          type: integer
          format: int64
        content:
          type: string
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    ChatPage:
      type: object
      required: [items, meta]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Chat'
        meta:
          $ref: '#/components/schemas/CursorPageMeta'

    MessagePage:
      type: object
      required: [items, meta]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Message'
        meta:
          $ref: '#/components/schemas/CursorPageMeta'

    # =====================
    # Credential Schemas
    # =====================
    CredentialType:
      type: string
      enum:
        # 사업자 및 면허
        - IDENTITY_VERIFICATION
        - SOLE_PROPRIETOR
        - CONSTRUCTION_LICENSE
        - SPECIALTY_CONSTRUCTION_LICENSE
        # 증명서
        - CAREER_CERTIFICATE
        - SKILL_GRADE_CERTIFICATE
        - OTHER_CERTIFICATE
        # 자격증
        - NATIONAL_TECHNICAL_QUALIFICATION
        - SKILLED_TECHNICIAN
        - OTHER_QUALIFICATION

    CredentialStatus:
      type: string
      enum:
        - PENDING
        - ACCEPTED
        - DENIED

    Credential:
      type: object
      properties:
        id:
          type: integer
          format: int64
        profileId:
          type: integer
          format: int64
        type:
          $ref: '#/components/schemas/CredentialType'
        status:
          $ref: '#/components/schemas/CredentialStatus'
        expiredAt:
          type: string
          format: date
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time

    CreateCredentialRequest:
      type: object
      required: [type]
      properties:
        type:
          $ref: '#/components/schemas/CredentialType'

    # =====================
    # Coworker Schemas
    # =====================
    Coworker:
      type: object
      properties:
        id:
          type: integer
          format: int64
        minId:
          type: integer
          format: int64
        maxId:
          type: integer
          format: int64

    # =====================
    # CoworkerRequest Schemas
    # =====================
    CoworkerRequest:
      type: object
      properties:
        id:
          type: integer
          format: int64
        fromId:
          type: integer
          format: int64
        toId:
          type: integer
          format: int64

    CreateCoworkerRequest:
      type: object
      required: [toId]
      properties:
        toId:
          type: integer
          format: int64
