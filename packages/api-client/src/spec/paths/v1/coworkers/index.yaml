get:
  operationId: getCoworkers
  tags:
    - Coworkers
  summary: 동료 목록 조회
  parameters:
    - name: profileId
      in: query
      required: true
      description: 프로필 ID
      schema:
        type: integer
        format: int64
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../../_shared/components/schemas/ApiSuccessResponseBase.yaml
              - type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      $ref: ../../../components/schemas/Coworker.yaml
