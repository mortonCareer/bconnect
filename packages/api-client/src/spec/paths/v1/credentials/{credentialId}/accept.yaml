post:
  operationId: acceptCredential
  tags:
    - Credentials
  summary: 인증뱃지 승인 (ADMIN)
  parameters:
    - name: credentialId
      in: path
      required: true
      schema:
        type: integer
        format: int64
  responses:
    '200':
      description: OK
