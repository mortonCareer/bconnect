post:
  operationId: denyCredential
  tags:
    - Credentials
  summary: 인증뱃지 거부 (ADMIN)
  parameters:
    - name: credentialId
      in: path
      required: true
      schema:
        type: integer
        format: int64
  responses:
    '200':
      description: OK
