name: KISCON Construction Sync

on:
  schedule:
    # 매주 월요일 01:00 UTC (10:00 KST) — 기존 kiscon-sync와 1시간 차이
    - cron: '0 1 * * 1'
  workflow_dispatch:
    inputs:
      full:
        description: 'Full sync (2003~present)'
        required: false
        default: 'false'
        type: boolean

jobs:
  sync:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/career
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        working-directory: .

      - name: Sync KISCON construction data to Postgres
        env:
          KISCON_API_SERVICE_KEY: ${{ secrets.KISCON_API_SERVICE_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ "${{ inputs.full }}" = "true" ]; then
            pnpm exec tsx scripts/kiscon-construction-sync.ts --full
          else
            pnpm exec tsx scripts/kiscon-construction-sync.ts
          fi

      - name: Notify Slack on failure
        if: failure()
        run: |
          curl -s -X POST "$SLACK_WEBHOOK_URL" \
            -H 'Content-Type: application/json' \
            -d '{"text":"🚨 *KISCON Construction Sync 실패*\n<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|워크플로우 로그 확인>"}'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Report failure to GitHub Issue
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          WORKFLOW_NAME="KISCON Construction Sync"
          EXISTING=$(gh issue list --label "🚨 sync-failure" --state open --search "$WORKFLOW_NAME" --json number -q '.[0].number')
          if [ -n "$EXISTING" ]; then
            gh issue comment "$EXISTING" --body "재실패: $RUN_URL"
          else
            gh issue create --title "🚨 ${WORKFLOW_NAME} 실패" \
              --label "🚨 sync-failure" \
              --body "Run: $RUN_URL"
          fi
