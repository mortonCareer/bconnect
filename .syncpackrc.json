{
  "$schema": "https://unpkg.com/syncpack@latest/dist/schema.json",

  "//": "버전 spec SSoT — 같은 dep 이 여러 workspace package 에 있을 때 unified declaration 강제.",
  "//1": "공유 dep 은 pnpm-workspace.yaml 의 catalog 사용 (https://pnpm.io/catalogs).",
  "//2": "새 dep 이 2+ package 에 들어가면 catalog 에 추가 + 본 versionGroup 의 dependencies 배열에 등록.",

  "versionGroups": [
    {
      "label": "workspace 내부 패키지는 항상 workspace:* 참조",
      "packages": ["**"],
      "dependencies": ["@bconnect/**"],
      "pinVersion": "workspace:*"
    },
    {
      "label": "공유 dep 은 catalog: 강제 (declaration SSoT)",
      "packages": ["**"],
      "dependencyTypes": ["dev", "prod", "peer"],
      "dependencies": [
        "@faker-js/faker",
        "@sentry/nextjs",
        "@types/node",
        "@types/react",
        "@types/react-dom",
        "eslint",
        "msw",
        "next",
        "react",
        "react-dom",
        "typescript",
        "zod",
        "zustand"
      ],
      "pinVersion": "catalog:"
    }
  ]
}
