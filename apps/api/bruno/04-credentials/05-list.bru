meta {
  name: 내 자격 증빙 목록 조회
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/credentials/me
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
  });
}

docs {
  로그인 멤버의 자격 증빙 목록을 조회한다.
}
