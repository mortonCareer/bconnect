meta {
  name: 제안 취소
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/offers/{{queuedOfferId}}/cancel
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  업체가 대기열의 제안(queuedOfferId)을 취소한다.
}
