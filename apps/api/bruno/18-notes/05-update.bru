meta {
  name: 노트 수정
  type: http
  seq: 5
  tags: [
    chain
    idempotent
  ]
}

put {
  url: {{baseUrl}}/api/v1/notes/{{noteId}}
  auth: inherit
}

body {
  {
    "content": "드라이브 노트 수정"
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 18-notes/01-create-by-drive
}
