meta {
  name: 드라이브 노트 목록 조회
  type: http
  seq: 3
  tags: [
    chain
  ]
}

get {
  url: {{baseUrl}}/api/v1/notes?driveId={{memberDriveId}}
  auth: inherit
}

params:query {
  driveId: {{memberDriveId}}
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
  });
}

docs {
  @chain
  - 17-drives/01-create-member
}
