meta {
  name: 동료 작업 목록 조회
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/coworkers/{{coworkerId}}/tasks
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
  });
}

docs {
  동료 멤버({{coworkerId}})의 작업 목록(바쁨 여부)을 조회한다.
  @chain
  - 13-coworker-requests/05-accept
}
