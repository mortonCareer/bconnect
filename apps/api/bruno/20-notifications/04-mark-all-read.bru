meta {
  name: 전체 읽음
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/notifications/read
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  로그인 멤버의 안읽은 알림을 모두 읽음 처리한다.
}
