meta {
  name: 내 프로필 소개 수정
  type: http
  seq: 5
  tags: [
    idempotent
  ]
}

put {
  url: {{baseUrl}}/api/v1/profiles/me/about
  auth: inherit
}

body {
  {
    "about": "새로운 소개"
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 03-profiles/01-create
}
