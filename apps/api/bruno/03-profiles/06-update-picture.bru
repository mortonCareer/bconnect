meta {
  name: 내 프로필 사진 수정
  type: http
  seq: 6
  tags: [
    idempotent
  ]
}

put {
  url: {{baseUrl}}/api/v1/profiles/me/picture
  auth: inherit
}

body {
  {
    "pictureId": 1
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @seed attachments : 1
  @chain
  - 03-profiles/01-create
}
