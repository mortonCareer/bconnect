meta {
  name: 프로필 사진 변경
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/api/v1/profiles/me/picture
  auth: inherit
}

body {
  {
    "pictureId": 1
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  로그인 사용자(member 1)의 프로필(profile 1) 사진을 시드 첨부(attachment 1, 본인 소유·COMPLETED)로 변경한다.
  @seed members : 1
  @seed profiles : 1
  @seed attachments : 1
}
