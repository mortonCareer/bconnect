meta {
  name: 받은 추천서 노출
  type: http
  seq: 7
  tags: [
    seed
  ]
}

post {
  url: {{baseUrl}}/api/v1/recommendations/1/show
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  수신자(profile 1)가 숨긴 추천서(seed rec 1)를 다시 노출한다.
  @seed members : 1
  @seed members : 6
  @seed profiles : 1
  @seed profiles : 5
  @seed recommendations : 1
}
