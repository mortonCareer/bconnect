meta {
  name: 받은 추천서 숨김
  type: http
  seq: 5
  tags: [
    seed
  ]
}

post {
  url: {{baseUrl}}/api/v1/recommendations/1/hide
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  수신자(profile 1)가 받은 추천서(seed rec 1)를 숨긴다.
  @seed members : 1, 6
  @seed profiles : 1, 5
  @seed recommendations : 1
}
