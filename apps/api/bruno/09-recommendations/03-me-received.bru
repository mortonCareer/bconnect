meta {
  name: 내가 받은 추천서 조회
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/recommendations/me/received
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
  });
}
