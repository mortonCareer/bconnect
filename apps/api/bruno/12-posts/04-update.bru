meta {
  name: 게시글 수정
  type: http
  seq: 4
  tags: [
    seed
    chain
    seed
    idempotent
  ]
}

put {
  url: {{baseUrl}}/api/v1/posts/{{postId}}
  body: json
  auth: inherit
}

body:json {
  {
    "taskId": {{workerTaskId}},
    "attachmentIds": [106],
    "content": "게시글 수정"
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @seed attachments : 106
  @chain
  - 11-worker-tasks/01-create-worker-task
  - 12-posts/01-create
}
