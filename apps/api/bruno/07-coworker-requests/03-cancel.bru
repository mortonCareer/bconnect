meta {
  name: 동료 요청 취소
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/coworker-requests/{{coworkerRequestId}}
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  profile 2에게 보낸 동료 요청을 취소한다.
}
