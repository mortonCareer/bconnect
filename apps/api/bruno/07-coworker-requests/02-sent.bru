meta {
  name: 보낸 동료 요청 조회
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/coworker-requests/sent
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
  });
}
