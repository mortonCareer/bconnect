meta {
  name: 동료 요청 거절
  type: http
  seq: 6
  tags: [
    seed
  ]
}

post {
  url: {{baseUrl}}/api/v1/coworker-requests/2/deny
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  profile 4가 보낸 동료 요청(seed)을 거절한다.
  @seed members : 1, 5
  @seed profiles : 1, 4
  @seed coworker_requests : 2
}
