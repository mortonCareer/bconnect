meta {
  name: 드라이브 첨부 삭제
  type: http
  seq: 11
  tags: [
    chain
  ]
}

delete {
  url: {{baseUrl}}/api/v1/drives/:id/attachments/:attachmentId
  auth: inherit
}

params:path {
  id: 100
  attachmentId: 110
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 18-member-drives/10-upload
}
