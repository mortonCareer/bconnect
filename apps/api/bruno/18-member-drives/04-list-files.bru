meta {
  name: 드라이브 파일 목록 조회
  type: http
  seq: 4
  tags: [
    chain
  ]
}

get {
  url: {{baseUrl}}/api/v1/drives/{{memberDriveId}}/files
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
  });
}

docs {
  @chain
  - 18-member-drives/01-create
}
