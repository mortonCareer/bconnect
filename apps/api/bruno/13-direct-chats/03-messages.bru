meta {
  name: 1:1 채팅 메시지 조회
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/direct-chats/{{directChatId}}/messages
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data.content).to.be.an('array');
  });
}

docs {
  생성한 1:1 채팅방(directChatId)의 메시지를 커서 페이지로 조회한다.
}
