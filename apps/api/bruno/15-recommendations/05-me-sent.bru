meta {
  name: 내가 보낸 추천서 목록 조회
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/recommendations/me/sent
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
  });
}
