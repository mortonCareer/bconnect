meta {
  name: 내 채팅방 목록 조회
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/group-chats
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
  });
}

docs {
  로그인 유저가 참여한 채팅방 목록을 조회한다.
}
