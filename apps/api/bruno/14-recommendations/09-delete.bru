meta {
  name: 추천서 삭제
  type: http
  seq: 9
  tags: [
    chain
    idempotent
  ]
}

delete {
  url: {{baseUrl}}/api/v1/recommendations/{{recommendationId}}
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 14-recommendations/01-create
}
