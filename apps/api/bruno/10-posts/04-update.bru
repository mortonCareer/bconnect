meta {
  name: 게시글 수정
  type: http
  seq: 4
  tags: [
    chain
    idempotent
  ]
}

put {
  url: {{baseUrl}}/api/v1/posts/{{postId}}
  auth: inherit
}

body {
  {
    "content": "게시글 수정"
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 09-worker-tasks/01-create-worker-task
  - 10-posts/01-create
}
