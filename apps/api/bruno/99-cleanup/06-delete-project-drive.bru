meta {
  name: 프로젝트 드라이브 삭제
  type: http
  seq: 6
  tags: [
    chain
    idempotent
  ]
}

delete {
  url: {{baseUrl}}/api/v1/drives/{{projectDriveId}}
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 05-companies/01-create
  - 06-projects/01-create
  - 10-project-drives/01-create
}
