meta {
  name: 프로젝트 삭제
  type: http
  seq: 8
  tags: [
    chain
    idempotent
  ]
}

delete {
  url: {{baseUrl}}/api/v1/projects/{{projectId}}
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 04-companies/01-create
  - 05-projects/01-create
}
