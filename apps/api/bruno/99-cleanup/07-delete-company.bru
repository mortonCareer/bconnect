meta {
  name: 내 업체 삭제
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/v1/companies/me
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}
