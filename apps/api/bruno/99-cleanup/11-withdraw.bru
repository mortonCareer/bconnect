meta {
  name: 회원 탈퇴
  type: http
  seq: 11
  tags: [
    idempotent
  ]
}

delete {
  url: {{baseUrl}}/api/v1/members/me
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}
