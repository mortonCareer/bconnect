meta {
  name: 게시글 삭제
  type: http
  seq: 3
  tags: [
    chain
    idempotent
  ]
}

delete {
  url: {{baseUrl}}/api/v1/posts/{{postId}}
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 11-worker-tasks/01-create-worker-task
  - 12-posts/01-create
}
