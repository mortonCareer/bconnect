meta {
  name: 동료 삭제
  type: http
  seq: 4
  tags: [
    seed
  ]
}

delete {
  url: {{baseUrl}}/api/v1/coworkers/1
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  로그인 유저(profile 1)가 시드 동료 관계(coworker 1 = profile1·profile5)를 삭제한다.
  @seed members : 1, 6
  @seed profiles : 1, 5
  @seed coworkers : 1
}
