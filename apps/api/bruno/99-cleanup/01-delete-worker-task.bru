meta {
  name: 기술자 작업 삭제
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/v1/tasks/{{workerTaskId}}
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}
