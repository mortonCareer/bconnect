meta {
  name: 로그아웃
  type: http
  seq: 10
  tags: [
    idempotent
  ]
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}
