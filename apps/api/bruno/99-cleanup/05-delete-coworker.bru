meta {
  name: 동료 삭제
  type: http
  seq: 5
  tags: [
    chain
    idempotent
  ]
}

delete {
  url: {{baseUrl}}/api/v1/coworkers/{{coworkerId}}
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  로그인 유저(member 100)가 수락으로 생성된 동료 member {{coworkerId}}를 삭제한다.
  @chain
  - 13-coworker-requests/05-accept
}
