meta {
  name: 동료 요청 승낙
  type: http
  seq: 5
  tags: [
    seed
  ]
}

post {
  url: {{baseUrl}}/api/v1/coworker-requests/1/accept
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  profile 3가 보낸 동료 요청(seed)을 승낙한다.
  @seed members : 1, 4
  @seed profiles : 1, 3
  @seed coworker_requests : 1
}
