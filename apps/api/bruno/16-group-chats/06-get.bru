meta {
  name: 채팅방 단건 조회
  type: http
  seq: 6
  tags: [
    chain
  ]
}

get {
  url: {{baseUrl}}/api/v1/group-chats/{{chatId}}
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data.id).to.be.a('number');
    expect(res.body.data.participants).to.be.an('array');
    expect(res.body.data.unreadCount).to.be.a('number');
  });
}

docs {
  @chain
  - 16-group-chats/01-create
}
