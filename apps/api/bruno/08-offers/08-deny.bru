meta {
  name: 제안 거절
  type: http
  seq: 8
  tags: [
    chain
  ]
}

post {
  url: {{baseUrl}}/api/v1/offers/{{denyOfferId}}/deny
  auth: bearer
}

auth:bearer {
  token: {{accessToken2}}
}

vars:pre-request {
  denyOfferId: 201
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 01-otp/04-verify-otp-login-2
  - 05-companies/01-create
  - 06-projects/01-create
  - 07-company-tasks/01-create-company-task
  - 08-offers/02-create-deny
}
