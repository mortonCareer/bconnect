meta {
  name: 섭외 취소
  type: http
  seq: 7
  tags: [
    chain
  ]
}

post {
  url: {{baseUrl}}/api/v1/offers/{{offerId}}/cancel
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 05-companies/01-create
  - 06-projects/01-create
  - 07-company-tasks/01-create-company-task
  - 08-offers/01-create
}
