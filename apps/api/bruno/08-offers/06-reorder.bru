meta {
  name: 섭외 순서 변경
  type: http
  seq: 6
  tags: [
    chain
    idempotent
  ]
}

put {
  url: {{baseUrl}}/api/v1/offers/reorder
  body: json
  auth: inherit
}

body:json {
  {
    "offerIds": [{{denyOfferId}}, {{acceptOfferId}}]
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @chain
  - 08-offers/02-create-deny
  - 08-offers/03-create-accept
}
