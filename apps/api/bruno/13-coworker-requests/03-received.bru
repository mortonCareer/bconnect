meta {
  name: 받은 동료 요청 목록 조회
  type: http
  seq: 3
  tags: [
    seed
  ]
}

get {
  url: {{baseUrl}}/api/v1/coworker-requests/received
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
  });
}

docs {
  @seed coworker_requests : 100
  @seed coworker_requests : 101
}
