meta {
  name: 게시글 수정
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v1/posts/{{postId}}
  auth: inherit
}

body {
  {
    "content": "게시글 수정"
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}
