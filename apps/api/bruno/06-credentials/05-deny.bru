meta {
  name: 자격 거절 (ADMIN)
  type: http
  seq: 5
  tags: [
    admin
  ]
}

post {
  url: {{baseUrl}}/api/v1/credentials/2/deny
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  ADMIN 권한(member 1)으로 시드 자격(credential 2, PENDING)을 거절한다.
  @seed members : 1
  @seed profiles : 1
  @seed credentials : 2
}
