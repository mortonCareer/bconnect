meta {
  name: 내 사진 수정
  type: http
  seq: 7
  tags: [
    seed
    idempotent
  ]
}

put {
  url: {{baseUrl}}/api/v1/members/me/picture
  body: json
  auth: inherit
}

body:json {
  {
    "pictureId": 100
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @seed attachments : 100
}
