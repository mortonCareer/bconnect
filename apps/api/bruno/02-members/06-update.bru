meta {
  name: 내 정보 수정
  type: http
  seq: 6
  tags: [
    idempotent
  ]
}

put {
  url: {{baseUrl}}/api/v1/members/me
  auth: inherit
}

body {
  {
    "name": "이름 수정",
    "pictureId": 1
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
  @seed attachments : 1
}
