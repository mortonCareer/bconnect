meta {
  name: 회원 전체 조회 (ADMIN)
  type: http
  seq: 6
  tags: [
    admin
  ]
}

get {
  url: {{baseUrl}}/api/v1/members
  auth: inherit
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
  });
}

docs {
  ADMIN 권한(member 1)으로 전체 회원 목록을 조회한다.
}
