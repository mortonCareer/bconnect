meta {
  name: OTP 발송
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/otp/send
}

body {
  {
    "phone": "01000000099"
  }
}

tests {
  test("성공", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}

docs {
    전화번호로 OTP를 발송한다
}