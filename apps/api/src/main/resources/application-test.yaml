spring:
  datasource:
    url: jdbc:tc:postgresql:latest:///test

  sql:
    init:
      schema-locations:
        - classpath:schema-postgresql.sql
        - classpath:schema-crawler-postgresql.sql

app:
  jwt:
    secret: test-secret-key-for-jwt-authentication-minimum-256-bits
    cookie-domain: localhost

  nts:
    service-key: test-service-key

  kcomwel:
    service-key: test-service-key

  s3:
    bucket: test-bucket

  cloudfront:
    key-pair-id: test-key-pair-id
    private-key: test-private-key

  sns:
    platform-application-arn: test-sns-arn