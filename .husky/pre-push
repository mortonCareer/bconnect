protected="^(main|dev)$"

while read -r local_ref local_sha remote_ref remote_sha; do
  remote_branch=${remote_ref#refs/heads/}
  if echo "$remote_branch" | grep -qE "$protected"; then
    echo "($remote_branch)에 직접 push할 수 없습니다."
    exit 1
  fi
done
